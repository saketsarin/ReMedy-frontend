window.innerWidth < 768 &&
  [].slice
    .call(document.querySelectorAll("[data-bss-disabled-mobile]"))
    .forEach(function (t) {
      t.classList.remove("animated"),
        t.removeAttribute("data-bss-hover-animate"),
        t.removeAttribute("data-aos");
    }),
  document.addEventListener(
    "DOMContentLoaded",
    function () {
      [].slice
        .call(document.querySelectorAll("[data-bss-hover-animate]"))
        .forEach(function (t) {
          t.addEventListener("mouseenter", function (t) {
            t.target.classList.add(
              "animated",
              t.target.dataset.bssHoverAnimate
            );
          }),
            t.addEventListener("mouseleave", function (t) {
              t.target.classList.remove(
                "animated",
                t.target.dataset.bssHoverAnimate
              );
            });
        });
    },
    !1
  ),
  $(document).ready(function () {
    $(".search").keyup(function () {
      var t = $(".search").val(),
        e =
          ($(".results tbody").children("tr"),
          t.replace(/ /g, "'):containsi('"));
      $.extend($.expr[":"], {
        containsi: function (t, e, n, a) {
          return (
            (t.textContent || t.innerText || "")
              .toLowerCase()
              .indexOf((n[3] || "").toLowerCase()) >= 0
          );
        },
      }),
        $(".results tbody tr")
          .not(":containsi('" + e + "')")
          .each(function (t) {
            $(this).attr("visible", "false");
          }),
        $(".results tbody tr:containsi('" + e + "')").each(function (t) {
          $(this).attr("visible", "true");
        });
      var n = $('.results tbody tr[visible="true"]').length;
      $(".counter").text(n + " item"),
        "0" == n ? $(".no-result").show() : $(".no-result").hide();
    });
  });
